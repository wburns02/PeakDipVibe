/** Shared className for inline error alerts (red border + icon + message). */
export const ERROR_ALERT =
  "flex items-center gap-2 rounded-xl border border-red/20 bg-red/5 px-4 py-3 text-sm text-red";
